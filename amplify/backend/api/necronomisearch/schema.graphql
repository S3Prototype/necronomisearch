type Book @model @auth(rules: [{allow: public}]) {
  id: ID!
  bookTitle: String!
  bookShortName: String!
  bookContentID: String!
}

type BookContent @model @auth(rules: [{allow: public}]) {
  id: ID!
  bookTitle: String!
  bookShortName: String!
  bookContent: String!
}
 